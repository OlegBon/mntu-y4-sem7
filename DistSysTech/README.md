# ⚙️ Технології розподілених систем та паралельних обчислень (ТРСтаПО)

📚 Предмет 7-го семестру МНТУ ім. Ю. Бугая  
🧵 Теми: паралельне програмування, розподілені обчислення, багатопоточність, асинхронність  
🔧 Мови реалізації: C#, C++, Java (Visual Studio 2022)

## 📁 Структура модуля

- `pr1/`, `pr2/`, ... — окремі практичні роботи (проєкти Visual Studio)
- `alias.txt` — альтернативні назви предмету (наприклад, "DistSysTech", "ТРСПО")
- `.gitignore` — виключення службових файлів IDE та build-артефактів
- `README.md` — опис предмету, принципи організації, список робіт

## 🚀 Практичні роботи (у процесі)

| Практична робота | Стан        | Опис                                                                                  |
| ---------------- | ----------- | ------------------------------------------------------------------------------------- |
| Практична №1     | ✅ виконано | Windows Forms додаток з потоками `BackgroundWorker` для сортування (C#)               |
| Практична №2     | ✅ виконано | Паралельні обчислення через MPI (C/C++)                                               |
| Практична №3     | ✅ виконано | Колективні операції та префіксна сума через MPI (C/C++)                               |
| Практична №4     | ✅ виконано | Паралельні області через OpenMP (C/C++)                                               |
| Практична №5     | ✅ виконано | Розподіл роботи між потоками через цикл for в OpenMP (C/C++)                          |
| Практична №6     | ✅ виконано | Опції OpenMP: `private`, `firstprivate`, `lastprivate`, `reduction`, `nowait` (C/C++) |
| Практична №8     | ✅ виконано | Організація багатозадачності за допомогою потоків на Java                             |

### 🧵 Практична №1 — Потоки через BackgroundWorker

**Тема:**  
Розробка програми демонстрації роботи з потоками виконання у Windows Forms.  
Сортування масиву алгоритмами вставки, бульбашкою та вибором.

**Мета:**  
Опанувати роботу з `BackgroundWorker` для реалізації паралельних обчислень у GUI-додатках.

**Функціонал:**

- Генерація масиву випадкових чисел заданої розмірності
- Сортування трьома алгоритмами у окремих потоках:
  - Вставка
  - Бульбашка
  - Вибір
- Візуалізація прогресу сортування (`ProgressBar`, `ListBox`)
- Вивід часу виконання кожного алгоритму (`Label`)
- Можливість зупинити потоки (`CancelAsync`)

**Інструменти:**  
Visual Studio 2022, .NET 8.0, Windows Forms, `BackgroundWorker`

**Результат:**  
Графічний додаток, що демонструє паралельне сортування з візуалізацією та таймінгом.

## ▶️ Приклад запуску

```bash
# Відкрити pr1.sln у Visual Studio 2022
# Запустити проект у режимі Debug або Release
```

> Після запуску: введіть розмір масиву → натисніть "Generate array" → "Sort" → спостерігайте за прогресом і часом

### 🧵 Практична №2 — Паралельні обчислення через MPI

**Тема:**  
Розробка програм з використанням MPI для демонстрації паралельної обробки даних та синхронної комунікації між вузлами.

**Мета:**  
Опанувати базові принципи роботи з MPI: розподіл задач між процесами, передача даних, централізований збір результатів, вимірювання часу виконання та оцінка прискорення.

**Завдання:**

- **Вправа 1.**  
  Реалізувати програму на C, яка обчислює суму чисел паралельно на різній кількості вузлів.  
  Виміряти час виконання паралельного сегменту за допомогою `MPI_Wtime`.  
  Провести запуск на 1, 2, 4 та 16 вузлах, зафіксувати час та обчислити прискорення.

  **Очікуваний вивід:**

  ```
  Вихід (на одному вузлі): Час виконання: ...
  Вихід (на двох вузлах): Час виконання: ..., Прискорення: ...
  Вихід (на чотирьох вузлах): Час виконання: ..., Прискорення: ...
  Вихід (на шістнадцяти вузлах): Час виконання: ..., Прискорення: ...
  ```

- **Вправа 2.**  
  Реалізувати програму, яка передає буфер масиву з усіх інших вузлів на один вузол (`rank 0`) за допомогою `MPI_Send` / `MPI_Recv`.  
  Вузол `rank 0` приймає всі буфери, об’єднує їх у один масив та виводить як єдину структуру.  
  Програма демонструє централізовану синхронну комунікацію між процесами.

**Інструменти:**  
Visual Studio 2022, MS-MPI, C (або C++), `MPI_Init`, `MPI_Comm_rank`, `MPI_Send`, `MPI_Recv`, `MPI_Wtime`

**Результат:**  
Консольні програми, що демонструють паралельне обчислення суми та централізовану передачу буферів у середовищі MPI.

### 🧵 Практична №3 — Колективні операції та префіксна сума через MPI

**Тема:**  
Розробка програм з використанням MPI для демонстрації колективних обчислень та обчислення префіксної суми.

**Мета:**  
Опанувати роботу з колективними операціями MPI (`MPI_Scatter`, `MPI_Reduce`, `MPI_Scan`) для розподілу даних, обчислення часткових результатів та їх агрегації.

**Завдання:**

- **Вправа 1.**  
  Розбити довгий вектор на підвектори однакової довжини.  
  Розподілити підвектори між процесами за допомогою `MPI_Scatter`.  
  Кожен процес обчислює свою часткову суму.  
  Зібрати часткові суми на кореневому вузлі (`rank 0`) за допомогою `MPI_Reduce`.

- **Вправа 2.**  
  Реалізувати програму для обчислення **префіксної суми** `n` чисел на `n` процесах.  
  Використати `MPI_Scan` для обчислення кумулятивної суми:
  ```
  y0 = x0
  y1 = x0 + x1
  y2 = x0 + x1 + x2
  ...
  ```

**Інструменти:**  
Visual Studio 2022, MS-MPI, C (або C++), `MPI_Scatter`, `MPI_Reduce`, `MPI_Scan`, `MPI_Init`, `MPI_Comm_rank`

**Результат:**  
Консольні програми, що демонструють розподіл даних, обчислення часткових сум та префіксної суми з використанням колективних операцій MPI.

### 🧵 Практична №4 — Паралельні області через OpenMP

**Тема:**  
Дослідження роботи паралельних областей у OpenMP, керування кількістю потоків, визначення версії стандарту та ролі майстер-нитки.

**Мета:**  
Опанувати базові принципи роботи з OpenMP: створення паралельних областей, контроль потоків, діагностика середовища та розуміння архітектури виконання.

**Завдання:**

- **Вправа 1.**  
  Реалізувати три приклади з використанням `#pragma omp parallel`, `num_threads(...)`, `omp_set_num_threads(...)` та `omp_set_dynamic(...)`.  
  Зафіксувати вивід потоків та пояснити різницю між глобальним і локальним керуванням.

- **Вправа 2.**  
  Визначити версію OpenMP, яку підтримує компілятор, за допомогою макросу `_OPENMP`.  
  Зафіксувати результат та зробити висновок щодо сумісності середовища.

- **Вправа 3.**  
  Запустити послідовну програму з директивою `#pragma omp parallel` та виміряти кількість реально активних потоків.  
  Пояснити, чому `omp_get_num_threads()` повертає різні значення залежно від місця виклику.

- **Вправа 4.**  
  Теоретично відповісти на запитання:
  - Чи може програма складатися лише з паралельних або лише з послідовних областей?
  - Чим відрізняється майстер-нитка від інших потоків?

**Інструменти:**  
Visual Studio 2022, MSVC (OpenMP 2.0), C++, `omp.h`, `omp_get_thread_num`, `omp_get_num_threads`, `omp_set_num_threads`, `#pragma omp parallel`

**Результат:**  
Консольні програми, що демонструють створення паралельних областей, контроль потоків, діагностику версії OpenMP та розуміння ролі майстер-нитки. Оформлено звіт з кодом, виводом та висновками.

### 🧵 Практична №5 — Розподіл роботи між потоками через цикл `for` в OpenMP

**Тема:**  
Дослідження механізмів розподілу циклічних ітерацій між потоками за допомогою директиви `#pragma omp for`.

**Мета:**  
Опанувати розпаралелювання циклів у OpenMP, зрозуміти поведінку потоків при статичному та динамічному розподілі, дослідити вплив кількості логічних ядер, а також дати теоретичні відповіді на ключові питання про розподіл ітерацій.

**Завдання:**

- **Вправа 1.**  
  Реалізувати програму з `#pragma omp for`, яка розподіляє 16 ітерацій між потоками.  
  Зафіксувати результат, проаналізувати кількість ітерацій на потік.  
  Вивід показує, що деякі потоки виконують по 2 ітерації, а деякі — по 1.

- **Вправа 2.**  
  Реалізувати програму з явним встановленням кількості потоків (`omp_set_num_threads`) та директивою `#pragma omp for`.  
  Пояснити, як OpenMP розподіляє ітерації між потоками залежно від компілятора, системи та налаштувань.

- **Вправа 3.**  
  Теоретичні відповіді на питання:
  - Чи можуть `omp_get_thread_num()` і `omp_get_num_threads()` повертати однакові значення?
  - Чи можна розподіляти ітерації без `#pragma omp for`?
  - Чи можна розподілити кілька циклів однією директивою?
  - Чи можливий рівномірний розподіл при `schedule(static)`?
  - Чи змінюється розподіл при повторному запуску програми?

**Інструменти:**  
Visual Studio 2022, MSVC (OpenMP 2.0), C++, `omp.h`, `omp_set_num_threads`, `omp_get_thread_num`, `omp_get_num_threads`, `#pragma omp for`

**Результат:**  
Консольні програми, що демонструють розподіл циклу між потоками, аналіз розподілу, теоретичні висновки щодо поведінки OpenMP. Оформлено звіт з кодом, виводом та поясненнями.

### 🧵 Практична №6 — Опції OpenMP: `private`, `firstprivate`, `lastprivate`, `reduction`, `nowait`

**Тема:**  
Дослідження поведінки змінних у паралельних циклах OpenMP при використанні директив `private`, `firstprivate`, `lastprivate`, `reduction`, `nowait`.

**Мета:**  
Опанувати механізми передачі значень між потоками, локалізації змінних, збирання результатів та уникнення бар’єрів синхронізації. Навчитись контролювати доступ до змінних у паралельних областях та ефективно розподіляти обчислення.

**Завдання:**

- **Приклад 1.**  
  Демонстрація `private` — кожен потік має власну копію змінної `i`, яка не впливає на глобальну область.

- **Приклад 2.**  
  Демонстрація `shared` — всі потоки мають доступ до однієї змінної `i`, що може призвести до конфліктів.

- **Приклад 3.**  
  Демонстрація `default(shared)` — всі змінні за замовчуванням спільні, якщо явно не вказано `private`.

- **Приклад 4.**  
  Використання `firstprivate` — кожен потік отримує ініціалізовану копію змінної з головного потоку.

- **Приклад 5.**  
  Використання `lastprivate` — значення змінної з останньої ітерації циклу зберігається після завершення паралельної області.

- **Приклад 6.**  
  Використання `reduction(+:result)` — кожен потік обчислює часткову суму, яка автоматично додається до глобального результату.

- **Приклад 7.**  
  Використання `nowait` — потоки не чекають завершення циклу, а одразу переходять до наступних інструкцій.

**Інструменти:**  
Visual Studio 2022, MSVC (OpenMP 2.0), C++, `omp.h`, `omp_set_num_threads`, `omp_get_thread_num`, `#pragma omp parallel`, `#pragma omp for`, `reduction`, `firstprivate`, `lastprivate`, `nowait`

**Результат:**  
Консольні програми, що демонструють поведінку змінних у паралельних циклах, механізми локалізації, збирання результатів та оптимізацію синхронізації. Оформлено звіт з кодом, виводом та стислими висновками по кожному прикладу.

### 🧵 Практична №8 — Організація багатозадачності на Java

**Тема:**
Організація багатозадачності за допомогою потоків на основі технології JAVA.

**Мета:**
Навчитися розробляти програмне забезпечення з використанням багатопоточності на основі мови програмування Java.

**Завдання:**

- **Приклад 1 (Створення потоків):**

  - Реалізація інтерфейсу `Runnable` (в т.ч. через лямбда-вираз).
  - Наслідування (розширення) класу `Thread`.
  - Демонстрація ключової різниці між прямим викликом `.run()` (виконання у головному потоці) та `.start()` (запуск нового потоку).

- **Приклад 2 (Синхронізація):**

  - Демонстрація "гонки даних" (race condition) при одночасному інкременті спільного лічильника (`Counter`) з багатьох потоків.
  - Використання ключового слова `synchronized` для захисту критичної секції (`counter++`) та забезпечення потокобезпечного результату (уникнення "стану гонитви").

- **Приклад 3 (Аналіз продуктивності):**

  - Порівняння часу виконання обчислювальної задачі в однопоточному (`BigTaskOneThread`) та багатопоточному (`BigTaskManyThreads`) режимах.
  - Використання `ExecutorService` (пулу потоків) та `Callable` для розпаралелювання обчислень і повернення результатів через об'єкти `Future`.
  - Розрахунок прискорення (Speedup = Time 1 / Time 2).

**Інструменти:**
VS Code, Java (JDK 17), `Thread`, `Runnable`, `Callable`, `Future`, `ExecutorService`, `synchronized`.

**Результат:**
Консольні програми, що демонструють основи створення потоків, вирішення гонки даних за допомогою синхронізації та аналіз прискорення (speedup) при використанні пулу потоків. Оформлено звіт з кодом, виводом та висновками.

## 🧩 Принципи організації

- **Ізольованість**: кожна практична — окремий проєкт з власним `.sln`
- **Сумісність**: підтримка C# та C++ у Visual Studio
- **Прозорість**: логування, коментарі в коді, README для кожної роботи
- **Архівність**: збереження всіх версій — від чернеток до фінальних рішень

## 📦 Залежності (буде уточнено)

- .NET SDK 6.0+
- Visual Studio 2022
- C++ Build Tools (опціонально)

_Модуль створено з метою практичного освоєння принципів паралельного програмування, розподілених обчислень та ефективного використання ресурсів у багатопотокових середовищах._
