# Практична робота №6: Забезпечення безпеки веб-застосунку "Облік вантажних перевезень"

Це захищена версія серверного веб-застосунку "Облік вантажних перевезень", розроблена в рамках 6-ї практичної роботи. Проєкт базується на функціоналі Практичної №5, але з фокусом на кібербезпеку.

**Тема:** _Захист веб-застосунку від поширених атак (SQL Injection, XSS, CSRF) та налаштування безпечного з'єднання._

## 🛡️ Опис змін та безпеки

Основна мета роботи — усунення вразливостей, виявлених у попередній версії. Тепер система є захищеною від основних векторів атак:

- **🔐 Аутентифікація:** Додано систему входу та реєстрації. Доступ до адмін-панелі мають лише авторизовані користувачі. Паролі зберігаються у зашифрованому вигляді (хеш `Bcrypt`).
- **💉 Anti-SQL Injection:** Всі запити до бази даних переписано з використанням **підготовлених виразів** (Prepared Statements). Це унеможливлює впровадження шкідливого SQL-коду.
- **🚫 Anti-XSS:** Всі дані, що виводяться на сторінку, проходять екранування через `htmlspecialchars`, що блокує виконання шкідливих JS-скриптів.
- **tokens Anti-CSRF:** Всі форми захищено унікальними токенами, що генеруються для кожної сесії. Це запобігає підробці запитів з інших сайтів.
- **🔒 HTTPS:** Налаштовано примусове перенаправлення на захищений протокол через `.htaccess`.

## 🛠️ Використані технології та інструменти

- **PHP (8.x)**: Серверна логіка, сесії, хешування паролів (`password_hash`).
- **MySQL (MariaDB)**: Реляційна база даних.
- **Apache**: Веб-сервер з модулем `mod_rewrite` та SSL.
- **Burp Suite**: Використовувався для тестування вразливостей та перевірки захисних механізмів.
- **HTML5 / CSS3 / JS**: Фронтенд.

## ✨ Ключові особливості реалізації

1.  **Архітектура безпеки**:
    - `auth_check.php`: Глобальний "охоронець", який перевіряє сесію та генерує CSRF-токени.
    - `db.php`: Підключення до нової бази даних.
    - `.htaccess`: Правила для `301 Redirect` на HTTPS та приховування розширень файлів.
2.  **Захист форм**:
    - Видалення записів перероблено з небезпечного `GET` запиту на захищений `POST` запит через форму.
    - Додано захист від повторної відправки форми (JS блокування кнопки).
3.  **База даних**:
    - Додано таблицю `users` для зберігання адміністраторів.

## 🗄️ Структура Бази Даних

База даних: `freight_transport_secure`

**1. Таблиця `requests` (Заявки):**

- `id` (PK), `client_name`, `cargo_description`, `weight`, `route_start`, `route_end`, `status`.

**2. Таблиця `users` (Адміністратори):**

- `id`: Унікальний ідентифікатор (PK).
- `username`: Логін (Unique).
- `password`: Хеш пароля (VARCHAR 255).
- `created_at`: Час реєстрації.

## 🚀 Як запустити (Localhost / XAMPP)

1.  **Налаштування сервера:** Переконайтеся, що XAMPP підтримує SSL (порт 443).
2.  **База даних:**
    - Створіть базу даних `freight_transport_secure`.
    - Імпортуйте SQL-скрипт (див. нижче).
3.  **Встановлення:**
    - Розмістіть папку проєкту `freight_secure` у `C:\xampp\htdocs\`.
4.  **Запуск:**
    - Перейдіть за адресою: [https://localhost/freight_secure/](https://www.google.com/search?q=https://localhost/freight_secure/)
    - _(Браузер може попередити про самопідписаний сертифікат — це нормально для локальної розробки, натисніть "Продовжити")._
5.  **Перший вхід:**
    - Перейдіть на `register.php`, щоб створити першого користувача.
    - Потім увійдіть через `login.php`.

### SQL для створення таблиць:

```sql
-- Створюємо базу даних
CREATE DATABASE IF NOT EXISTS freight_transport_secure;
USE freight_transport_secure;

-- Таблиця користувачів (адмінів)
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL, -- Тут зберігається хеш
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблиця заявок
CREATE TABLE requests (
    id INT AUTO_INCREMENT PRIMARY KEY,
    client_name VARCHAR(100) NOT NULL,
    cargo_description TEXT NOT NULL,
    weight FLOAT NOT NULL,
    route_start VARCHAR(255) NOT NULL,
    route_end VARCHAR(255) NOT NULL,
    status ENUM('Нова', 'В дорозі', 'Виконана') DEFAULT 'Нова',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Тестовий запис заявки
INSERT INTO requests (client_name, cargo_description, weight, route_start, route_end)
VALUES ('ТОВ "Сек'юрітіЛог"', 'Серверне обладнання', 1.2, 'Київ', 'Одеса');

```
