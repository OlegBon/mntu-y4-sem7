# Практична робота №5: Вебзастосунок "Облік вантажних перевезень"

Це серверний вебзастосунок, розроблений у рамках 5-ї практичної роботи з курсу "Веб-орієнтована розробка програмного забезпечення".

**Тема варіанту 1:** _Облік заявок на вантажні автомобільні перевезення з можливістю додавання, редагування та видалення записів у базі даних MySQL._

## 🚛 Опис проєкту

Застосунок являє собою CRM-систему для логістичних менеджерів. Він дозволяє вести облік заявок на перевезення, зберігаючи дані у реляційній базі даних. Система реалізує повний цикл **CRUD** (Create, Read, Update, Delete):

- **Перегляд:** Таблиця всіх активних заявок із відображенням клієнта, вантажу, маршруту та статусу.
- **Створення:** Форма для додавання нових замовлень.
- **Редагування:** Можливість змінити дані або статус заявки (наприклад, з "Нова" на "В дорозі").
- **Видалення:** Безповоротне видалення заявки з бази даних.

## 🛠️ Використані технології

- **PHP (8.x)**: Серверна мова програмування для обробки запитів та бізнес-логіки.
- **MySQL (MariaDB)**: Реляційна база даних для зберігання інформації.
- **Apache**: Веб-сервер (у складі XAMPP).
- **HTML5 / CSS3**: Семантична верстка та адаптивна стилізація (Mobile First).
- **JavaScript (ES6+)**: Клієнтська логіка для модальних вікон та валідації форм.

## ✨ Ключові особливості

- **Архітектура**: Проєкт розділено на логічні блоки: підключення до БД (`db.php`), скрипти обробки (`create.php`, `edit.php`, `delete.php`) та статику (`css/`, `js/`).
- **Безпека та Валідація**:
  - Використання `mysqli_real_escape_string` для захисту від SQL-ін'єкцій.
  - Приведення числових типів (`floatval`, `intval`).
  - Блокування кнопки "Оновити", якщо користувач не вніс жодних змін у форму (JS).
- **UX/UI (Користувацький досвід)**:
  - **Адаптивність**: Таблиця отримує горизонтальну прокрутку на мобільних, а форми розтягуються на весь екран для зручності.
  - **Кастомне модальне вікно**: Підтвердження видалення реалізовано через власне стилізоване вікно, а не стандартний `confirm()`.
  - **SEO-friendly URL**: Налаштовано файл `.htaccess` для приховування `index.php` з адреси та перенаправлень.

## 🗄️ Структура Бази Даних

Таблиця `requests`:

- `id`: Унікальний ідентифікатор (Primary Key).
- `client_name`: Назва замовника.
- `cargo_description`: Опис вантажу.
- `weight`: Вага вантажу (float).
- `route_start` / `route_end`: Маршрут.
- `status`: Статус заявки (ENUM: 'Нова', 'В дорозі', 'Виконана').

## 🚀 Як запустити (Localhost / XAMPP)

1.  **Встановіть XAMPP** (або аналогічний локальний сервер).
2.  **Запустіть** модулі **Apache** та **MySQL** у панелі керування XAMPP.
3.  **Налаштування БД:**
    - Відкрийте `http://localhost/phpmyadmin`.
    - Створіть базу даних з назвою `freight_transport`.
    - Виконайте SQL-запит для створення таблиці (код нижче).
4.  **Встановлення проєкту:**
    - Скопіюйте папку `freight_app` у директорію `C:\xampp\htdocs\`.
5.  **Запуск:**
    - Відкрийте у браузері: [http://localhost/freight_app/](http://localhost/freight_app/)

### SQL для створення таблиці:

```sql
-- Створюємо базу даних
CREATE DATABASE freight_transport;

-- Вибираємо її для роботи
USE freight_transport;

-- Створюємо таблицю заявок
CREATE TABLE requests (
    id INT AUTO_INCREMENT PRIMARY KEY,      -- Унікальний номер заявки
    client_name VARCHAR(100) NOT NULL,      -- Ім'я клієнта
    cargo_description TEXT NOT NULL,        -- Опис вантажу
    weight FLOAT NOT NULL,                  -- Вага (у тонах або кг)
    route_start VARCHAR(255) NOT NULL,      -- Звідки
    route_end VARCHAR(255) NOT NULL,        -- Куди
    status ENUM('Нова', 'В дорозі', 'Виконана') DEFAULT 'Нова', -- Статус
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP -- Час створення
);

-- Додамо один тестовий запис, щоб таблиця не була пустою
INSERT INTO requests (client_name, cargo_description, weight, route_start, route_end)
VALUES ('ТОВ "БудМаш"', 'Будівельні матеріали (цегла)', 5.5, 'Київ', 'Львів');

```
