## ⚙️ Веб орієнтована розробка програмного забезпечення (ВОРПЗ)

📚 Предмет 7-го семестру МНТУ ім. Ю. Бугая  
🌐 Напрям: клієнтська веб-розробка, HTML/CSS/JS  
🧵 Теми: розмітка, стилізація, інтерактивність, семантика, адаптивність  
🔧 Інструменти: VS Code, HTML5, CSS3, JavaScript, Vite, React

## 📁 Структура модуля

- `pr1/`, `pr2/`, ... `pr4/` — окремі практичні роботи
- `m1/` — модульна робота
- `alias.txt` — альтернативні назви предмету (наприклад, "WebDev", "FE", "CSD")
- `.gitignore` — виключення службових файлів IDE, кешу, білдів
- `README.md` — опис предмету, принципи організації, список робіт

## 🚀 Практичні роботи

| Практична робота   | Стан        | Опис                                                       |
| ------------------ | ----------- | ---------------------------------------------------------- |
| Практична №1       | ✅ виконано | Вступ до HTML і CSS                                        |
| Практична №2       | ✅ виконано | Інтерактивність на веб-сторінці з використанням JavaScript |
| Практична №3       | ✅ виконано | Галерея зображень з jQuery                                 |
| Модульна робота №1 | ✅ виконано | Односторінкові застосунки (HTML/JS/jQuery + React)         |
| Практична №4       | ✅ виконано | SPA "Керування роботом" (React, Hooks, Router, Fetch)      |
| Практична №5       | ✅ виконано | Backend на PHP + MySQL (CRUD, XAMPP)                       |

---

### 🧵 Практична №1 — Вступ до HTML і CSS

**Тема:**  
Створення веб-сторінки про улюблене місто з використанням базових HTML-елементів та стилізацією через CSS.

**Мета:**  
Ознайомлення з основами HTML-розмітки та стилізації елементів за допомогою CSS. Формування уявлення про структуру веб-документа.

**Функціонал:**

- Заголовки, абзаци, списки (`<h1>`, `<p>`, `<ul>`, `<ol>`)
- Гіперпосилання (`<a>`) та зображення (`<img>`)
- Семантична структура (`<header>`, `<main>`, `<footer>`)
- Зовнішній CSS-файл для стилізації

**Інструменти:**

- Visual Studio Code
- HTML5
- CSS3
- Статичне зображення `kharkiv.jpg`

**Результат:**  
Створено веб-сторінку про Харків з семантичною структурою, стилізованими елементами та інтерактивним посиланням.  
Файли: `laba1.html`, `styles.css`, `kharkiv.jpg`

## ▶️ Приклад запуску

Відкрити `laba1.html` у браузері.  
Сторінка містить заголовок, текст, списки, зображення та посилання на офіційний сайт міста.

## 📦 Залежності

_На даному етапі залежностей немає — чистий HTML/CSS._  
У наступних роботах можливе використання:

- JavaScript
- npm/yarn
- Webpack/Vite

---

### 🧮 Практична №2 — Інтерактивність на веб-сторінці з використанням JavaScript

**Тема:**  
Розробка інтерактивного калькулятора з використанням HTML, CSS та JavaScript.

**Мета:**  
Опанування базових принципів DOM-маніпуляцій, обробки подій, валідації вводу та реалізації логіки на JavaScript. Формування навичок створення інтерактивних веб-компонентів.

**Функціонал:**

- Виведення виразу та результату через `input`-поле
- Кнопки для цифр, операторів та функцій (`C`, `=`)
- Валідація вводу: заборона подвійних операторів (`++`, `--`, `*/` тощо)
- Обробка клавіатурних подій (`Enter`, `Backspace`, `Escape`)
- Підсвічування кнопок при натисканні (`hover`, `active`)
- Адаптивна grid-сітка для мобільних і десктопних пристроїв

**Інструменти:**

- Visual Studio Code
- HTML5
- CSS3 (Grid, Media Queries)
- JavaScript (DOM, Events, Validation)

**JS реалізовано:**

- `append(value)` — додає символ до виразу з перевіркою на подвійні оператори
- `calculate()` — обчислює вираз, обробляє помилки та ділення на нуль
- `clearDisplay()` — очищає поле вводу
- Обробка клавіатури — підтримка цифрових клавіш, операторів, `Enter`, `Backspace`, `Escape`
- Підсвічування кнопок — клас `active` з таймером
- Валідація — заборона некоректного вводу

**Результат:**  
Створено адаптивний калькулятор з базовими математичними операціями, валідацією та інтерактивною поведінкою.  
Файли: `index.html`, `style.css`, `script.js`

**Висновки:**

- Реалізовано повноцінний веб-компонент з логікою, стилізацією та адаптацією
- Забезпечено UX-дружню поведінку через обробку клавіатури та підсвічування
- Код структурований, масштабований, готовий до інтеграції в більші проєкти
- Практична робота демонструє навички клієнтської логіки, валідації та UI-розробки

---

### 🖼️ Практична №3 — Галерея зображень з jQuery

**Тема:**  
Розробка веб-застосунку з використанням jQuery для створення інтерактивної галереї зображень.

**Мета:**  
Ознайомлення з можливостями бібліотеки jQuery: маніпуляції з DOM, анімації, обробка подій. Формування навичок створення динамічного інтерфейсу.

**Функціонал:**

- Плавне зникнення зображення при кліку (`fadeOut`)
- Кнопка “Показати всі” — повертає приховані зображення (`fadeIn`)
- Динамічне додавання підписів до зображень (`alt`)
- Hover-ефекти через jQuery (`box-shadow`)
- Анімація появи зображень при завантаженні (`delay + fadeIn`)

**Інструменти:**

- Visual Studio Code
- HTML5
- CSS3
- JavaScript
- jQuery 3.6.0 (CDN)

**Результат:**  
Створено інтерактивну галерею з 8 зображень, які реагують на дії користувача.  
Файли: `index.html`, `styles.css`, `script.js`, `img/photo1.png` … `photo8.png`

**Висновки:**

- Реалізовано динамічну поведінку елементів через jQuery
- Забезпечено плавні анімації та UX-дружню взаємодію
- Код структурований, легко розширюється, готовий до інтеграції в більші проєкти

---

### 📘 Модульна робота №1 — Односторінкові застосунки (HTML/JS/jQuery + React)

**Тема:**  
Розробка двох SPA-застосунків:

1. Система обліку рейсів суден (HTML + CSS + JavaScript + jQuery)
2. Список завдань (React + Redux Toolkit)

**Мета:**  
Опанування двох підходів до веб-розробки — класичного та фреймворкового. Формування навичок адаптивної верстки, DOM-взаємодії, управління станом, валідації та збереження даних у localStorage.

---

#### ⚓ Завдання 1 — Система обліку рейсів суден (HTML/JS/jQuery)

**Функціонал:**

- Адаптивна верстка через Flexbox
- Слайдер зображень суден
- Форма планування рейсу з валідацією
- Таблиця запланованих рейсів
- Збереження та видалення рейсів через localStorage
- Анімації через jQuery (`fadeIn`, `fadeOut`)

**Файли:**  
`index.html`, `styles.css`, `script.js`, `img/ship1.jpg` … `ship7.jpg`

---

#### ✅ Завдання 2 — Список завдань (React + Redux Toolkit)

**Функціонал:**

- Додавання, видалення, очищення завдань
- Управління станом через Redux Toolkit (`createSlice`)
- Збереження завдань у localStorage
- Компоненти `TodoList`, `Task`
- Валідація вводу, `checked` + `onChange`
- Унікальні ключі для списків (`key`)
- Оновлення до React 19.2.0, Redux Toolkit 2.9.2, npm 11.6.2
- Аудит безпеки через `npm audit`, усунення вразливостей

**Файли:**  
`src/App.js`, `TodoList.jsx`, `Task.jsx`, `Redux/TodoListSlice.js`, `package.json`

---

**Висновки:**  
Модульна робота демонструє вміння працювати з класичними та сучасними інструментами веб-розробки. Реалізовано два SPA з адаптивним інтерфейсом, валідацією, управлінням станом та збереженням даних. Проведено оновлення середовища, аудит безпеки та рефакторинг компонентів.

---

### **🤖 Практична №4 — SPA-застосунок з React**

**Тема:** Розробити односторінковий застосунок для керування автономним роботом на основі картографічних даних (Варіант 1).

**Мета:** Ознайомлення з фреймворком React, створення SPA з використанням компонентів, хуків (`useState`, `useEffect`), маршрутизації (`react-router-dom`) та взаємодії з API (`fetch`).

**Функціонал:**

- **Архітектура SPA**: Проєкт створено на **Vite** (замість застарілого `create-react-app`).
- **Роутинг**: Реалізовано перехід між сторінками "Моніторинг" (`/`) та "Журнал завдань" (`/log`) за допомогою `react-router-dom` без перезавантаження.
- **Централізований стан**: Використано патерн "Підняття стану". Весь стан (`robotsData`, `logs`, `isBusy`) зберігається в `App.jsx`, що дозволяє зберегти його при переході між сторінками.
- **Імітація API**:
  - `fetchRobotsData`: Асинхронно завантажує дані про роботів з `public/robots.json` за допомогою `fetch`.
  - `sendRobotCommand`: Імітує `POST`-запит через `Promise`, повертаючи оновлений стан робота.
- **"Розумна" логіка**:
  - **Батарея**: Розраховується динамічно (`distance * 0.1`).
  - **Авто-повернення**: Робот автоматично повертається на базу, якщо заряд нижче 30%.
  - **Статус "Stranded"**: Відображається, якщо заряд 0%.
- **Динамічний UI**:
  - Карта з рухомими іконками роботів (позиція береться зі стану).
  - Кнопки керування динамічно блокуються (`disabled`) залежно від 4-х правил (зайнятий, на базі, низький заряд тощо).
- **Журнал**: Всі дії системи та роботів записуються в `logs` і відображаються на окремій сторінці.

**Інструменти:**

- Vite
- React 19 (Hooks: `useState`, `useEffect`)
- react-router-dom
- Fetch API
- CSS (Grid, Flexbox, Transitions)

**Результат:** Створено повноцінний, інтерактивний SPA-дашборд, що симулює керування кількома роботами, коректно керує станом та демонструє засвоєння всіх ключових аспектів сучасної React-розробки.

**Файли:** `src/App.jsx`, `src/components/DashboardPage.jsx`, `src/components/LogPage.jsx`, `src/components/RobotMap.jsx`, `src/api/robot-api.js`, `public/robots.json`

---

### **🚛 Практична №5 — Вебзастосунок "Облік вантажних перевезень"**

**Тема:** Облік заявок на вантажні автомобільні перевезення з можливістю додавання, редагування та видалення записів у базі даних MySQL (Варіант 1).

**Мета:** Ознайомлення з основами серверної розробки на PHP, взаємодією з базами даних MySQL (через `mysqli`) та реалізацією повного циклу CRUD-операцій.

**Функціонал:**

- **CRUD-операції**:
  - **Create**: Форма додавання нових заявок із захистом від SQL-ін'єкцій.
  - **Read**: Виведення списку заявок у таблицю зі статусами.
  - **Update**: Редагування існуючих записів із попереднім заповненням полів.
  - **Delete**: Видалення записів.
- **UX/UI покращення**:
  - **Адаптивність**: Таблиця з горизонтальним скролом та форми на весь екран для мобільних пристроїв.
  - **JS-валідація**: Кнопка "Оновити" неактивна, доки користувач не змінить дані у формі.
  - **Модальне вікно**: Кастомне вікно підтвердження видалення (без використання `confirm()`).
- **Налаштування сервера**: Використання `.htaccess` для SEO-friendly URL (приховування `index.php`).

**Інструменти:**

- XAMPP (Apache + MySQL + PHP 8.x)
- PHP (процедурний підхід)
- MySQL / MariaDB
- HTML5 / CSS3 (Mobile First)
- JavaScript (DOM manipulation)

**Результат:** Розроблено повноцінний CRM-інструмент для логістики, який працює на локальному сервері, коректно відображається на мобільних пристроях та забезпечує безпечну взаємодію з базою даних.

**Файли:** `index.php`, `create.php`, `edit.php`, `delete.php`, `db.php`, `.htaccess`, `css/style.css`, `js/form.js`, `js/modal.js`

_Модуль створено для практичного освоєння основ веб-розробки, структурування контенту та стилізації інтерфейсів. Кожна робота — окремий проєкт з власною структурою та документацією._
